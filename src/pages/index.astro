---
const photo = "/images/headshot-tan tee-2.jpg";
import Layout from "../layouts/Layout.astro";
import Testimonials from "../components/Testimonials.astro";
import SkillsCard from "../components/SkillsCard.astro";
---

<Layout fullWidth={true}>
  <!-- Hero Section (Full Width) -->
  <div class="hero-wrapper">
    <section
      class="hero relative flex flex-col items-center justify-center min-h-screen overflow-hidden w-full"
    >
      <!-- Full-width Background Effects for Dark Mode and Light Mode -->
      <div class="absolute inset-0 z-0 w-full h-full">
        <!-- Dark Mode Background -->
        <div
          class="absolute inset-0 w-full h-full bg-gradient-to-b from-gray-100 to-gray-300 dark:from-gray-800 dark:to-gray-900"
        >
        </div>

        <!-- Light Mode Background Effects -->
        <div
          class="absolute top-1/4 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-[500px] h-[500px] bg-gradient-to-br from-blue-200 to-purple-300 dark:hidden rounded-full opacity-30 blur-2xl"
        >
        </div>
        <div
          class="absolute bottom-1/4 right-1/2 transform translate-x-1/2 translate-y-1/2 w-[400px] h-[400px] bg-gradient-to-br from-pink-200 to-red-300 dark:hidden rounded-full opacity-30 blur-2xl"
        >
        </div>
      </div>

      <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 w-full">
        <!-- Profile Image Section -->
        <div class="relative w-[400px] h-[400px] mb-4 mx-auto">
          <img
            src={photo}
            alt="Matt Birch"
            class="w-full h-full object-cover rounded-full shadow-2xl"
          />
          <h1
            class="absolute bottom-[-40px] left-1/2 transform -translate-x-1/2 text-8xl font-extrabold z-20 text-teal-900"
          >
            Matt Birch
          </h1>
        </div>

        <!-- Text to the left and right of the image -->
        <div
          class="absolute w-full top-1/3 transform -translate-y-1/2 flex items-center justify-between px-12 z-0"
        >
          <p class="text-3xl font-bold rotate-[-15deg] hero-text">I'm Hero!</p>
          <p class="text-3xl font-bold rotate-[15deg] hero-text">
            You know it!
          </p>
        </div>

        <!-- Additional Info and Button -->
        <div class="text-center mt-4 z-20">
          <p
            class="text-2xl font-semibold mb-4 leading-relaxed max-w-2xl mx-auto hero-text"
          >
            I'm a web developer who creates beautiful and functional websites.
            Let's build something together.
          </p>
          <a
            href="#contact"
            class="inline-block bg-blue-600 hover:bg-blue-700 text-white py-4 px-10 rounded-full shadow-lg transition-all duration-300"
          >
            Get In Touch
          </a>
        </div>
      </div>
    </section>
  </div>

  <!-- Unique Sales Proposition Section (Contained) -->
  <section class="py-24">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="text-3xl font-bold mb-6">What Sets Me Apart</h2>
      <p class="text-lg mb-4">
        As a front-end web developer, I bring a unique blend of technical
        expertise and creative problem-solving to every project. Here's what
        differentiates me:
      </p>
      <ul class="list-disc list-inside space-y-2">
        <li>Mastery of modern web technologies and frameworks</li>
        <li>User-centric design approach for intuitive interfaces</li>
        <li>Strong focus on performance optimization and accessibility</li>
        <li>Collaborative mindset and excellent communication skills</li>
      </ul>
    </div>
  </section>

  <div aria-hidden="true" class="my-12 w-full max-w-3xl mx-auto">
    <div
      class="h-px bg-gradient-to-r from-transparent via-gray-400 dark:via-gray-600 to-transparent"
    >
    </div>
  </div>

  <!-- Testimonials Section -->
  <section class="py-24">
    <h2 class="text-4xl font-bold text-center mb-12">Testimonials</h2>
    <div class="testimonials-div bg-gray-900 py-12">
      <div
        class="testimonials-container max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"
      >
        <Testimonials />
      </div>
    </div>
  </section>

  <!-- About Me / What I Can Do for Clients Section (Contained) -->
  <section class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
    <h2 class="text-3xl font-bold mb-6">What I Can Do for You</h2>
    <p class="text-lg mb-4">
      My goal is to transform your digital presence and help your business
      thrive online. Here's how I can help:
    </p>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
      <div>
        <h3 class="text-xl font-semibold mb-2">Custom Web Development</h3>
        <p>
          Tailor-made websites that perfectly align with your brand and business
          goals.
        </p>
      </div>
      <div>
        <h3 class="text-xl font-semibold mb-2">Responsive Design</h3>
        <p>
          Ensure your website looks great and functions flawlessly on all
          devices.
        </p>
      </div>
      <div>
        <h3 class="text-xl font-semibold mb-2">Performance Optimization</h3>
        <p>
          Speed up your website for better user experience and improved SEO
          rankings.
        </p>
      </div>
      <div>
        <h3 class="text-xl font-semibold mb-2">Ongoing Support</h3>
        <p>
          Regular maintenance and updates to keep your website secure and
          up-to-date.
        </p>
      </div>
    </div>
  </section>

  <!-- Skills Section (Full Width) -->
  <section class="skills-section py-16 w-full transition-colors duration-300">
    <div class="mx-auto">
      <h2 class="text-4xl font-bold text-center mb-12 skills-title">Skills</h2>
      <div class="skills-container mx-auto">
        <SkillsCard />
      </div>
    </div>
  </section>

  <!-- Call to Action / Contact Section (Contained) -->
  <section id="contact" class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
    <h2 class="text-3xl font-bold mb-6">Let's Work Together</h2>
    <p class="text-lg mb-8">
      Ready to take your web presence to the next level? I'm here to help you
      create a stunning, functional website that drives results. Get in touch
      today, and let's discuss how we can bring your vision to life.
    </p>
    <form class="space-y-4">
      <div>
        <label for="name" class="block text-sm font-medium text-gray-700"
          >Name</label
        >
        <input
          type="text"
          id="name"
          name="name"
          class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50"
        />
      </div>
      <div>
        <label for="email" class="block text-sm font-medium text-gray-700"
          >Email</label
        >
        <input
          type="email"
          id="email"
          name="email"
          class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50"
        />
      </div>
      <div>
        <label for="message" class="block text-sm font-medium text-gray-700"
          >Message</label
        >
        <textarea
          id="message"
          name="message"
          rows="4"
          class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50"
        ></textarea>
      </div>
      <div>
        <button
          type="submit"
          class="inline-flex items-center px-4 py-2 border border-transparent text-base font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"
        >
          Send Message
        </button>
      </div>
    </form>
  </section>

  <style>
    .hero-wrapper {
      margin-top: -4rem;
      margin-bottom: -4rem;
    }

    .hero {
      position: relative;
      z-index: 1;
    }

    :global(.dark) .hero-text {
      color: #ffffff;
    }

    .hero > div {
      position: relative;
      z-index: 2;
    }

    .testimonials-div {
      position: relative;
      z-index: 1;
      background-color: #1e293b; /* Dark grey background (Tailwind's gray-800) for light mode */
      border: 2px solid #2d3748; /* Add a border for better visibility */
      border-radius: 12px; /* Optional: to give it a rounded appearance */
    }

    :global(.dark) .testimonials-section {
      background-color: #1f2937; /* Tailwind's gray-800 */
      border: 2px solid #4b5563; /* Add a lighter border for dark mode */
      border-radius: 12px; /* Optional: to match the rounded appearance */
    }

    .skills-title {
      color: #1a202c; /* Dark color for light mode */
    }

    :global(.dark) .skills-title {
      color: #ffffff; /* Light color for dark mode */
    }

    .testimonials-container {
      margin-top: 2rem; /* Add some space between the title and the container */
    }
  </style>
</Layout>
